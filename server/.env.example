# Server
PORT=4000
MONGO_URI=mongodb://127.0.0.1:27017/ganesh_journal
JWT_SECRET=super-secret-change-me
UPLOAD_DIR=uploads
CORS_ORIGIN=http://localhost:5173

# Optional: External APIs
FOREX_API_KEY=
ECONOMIC_CALENDAR_API_URL=

# GPT (Vision) Trade Insights
# Create a key from OpenAI and set it here. Keep it server-side (do NOT put in frontend).
OPENAI_API_KEY=
# Vision-capable model recommended. Examples: gpt-4o-mini, gpt-4o
OPENAI_MODEL=gpt-4o-mini

# Optional: OpenAI-compatible base URL
# - For OpenAI: leave empty (default https://api.openai.com/v1)
# - For OpenRouter keys (sk-or-...): set https://openrouter.ai/api/v1
OPENAI_BASE_URL=https://openrouter.ai/api/v1


# Optional: shown in OpenRouter dashboard
OPENAI_APP_NAME=TradeJournal
# Optional: shown in OpenRouter dashboard (your site URL)
OPENAI_HTTP_REFERER=
